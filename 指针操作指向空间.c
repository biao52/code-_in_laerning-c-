//#include<stdio.h>
//int main(void)
//{
//	int a = 12;
//	int *p = &a;//此处的*是指针标识
//	//读
//	printf("%p %p\n", p, *p);//此处的*是内存操作运算符 *p读取的是p的地址对应空间的数值 
//	//*之后的地址必须是经过申请的地址
//	//p指向一个变量的地址 那么*p指的就是变量本身
//	//写
//	*p = 234;//相当于对a这个变量重新赋值234
//	printf("%p %p", p, *p);
//	printf("%p %p %p", p, &*p, &a);
//	return 0;
//}